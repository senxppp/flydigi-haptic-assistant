# 震动小助手 — 使用说明

## 快速开始

双击 **`震动小助手.exe`**，点一下中间那个大按钮「**启 动**」。

启动后同时生效两项功能：

| 功能 | 说明 |
|---|---|
| **音频震动** | 游戏里的音效（爆炸、脚步、环境音）实时转成握把震动 |
| **滑索扳机震动** | 检测到滑索动作时，握把持续震动，跟随滑索时长 |

再点一下按钮变「**停 止**」，所有震动立即关闭。

## 界面说明

```
┌──────────────────────────────────────┐
│ 震动小助手                  ● 运行中  │
├──────────────────────────────────────┤
│         ┌──────────────┐             │
│         │    启 动     │  ← 总开关   │
│         └──────────────┘             │
│  点击后同时开启：音频震动 + 滑索扳机震动│
├──────────────────────────────────────┤
│ 实时状态                              │
│   左握把  ████████░░░░░░░░  142       │
│   右握把  ██████░░░░░░░░░░  108       │
│   发送 60.0 Hz | 单帧 0.98 ms |       │
│   手柄 已连接 | 滑索：待机             │
├──────────────────────────────────────┤
│ 参数调节                              │
│   音频强度  ├────●──────┤    1.00     │
│   滑索力度  ├────────●──┤      75     │
├──────────────────────────────────────┤
│ 运行日志                              │
│   16:21:47  滑索开始 (第1组信号)      │
│   16:21:54  滑索结束 时长 6.46s       │
└──────────────────────────────────────┘
```

## 参数调节

| 参数 | 范围 | 说明 |
|---|---|---|
| **音频强度** | 0.2 ~ 2.5 | 游戏音效的整体震动强度。觉得震得太弱就调大 |
| **滑索力度** | 0 ~ 255 | 滑索时的震动力度。**默认 75**，是实测下来不盖过冲击感的值 |

参数改动**立即生效**，不用重启。

## 配置文件

首次运行会在 exe 旁边生成 `configs/config.json`，里面是完整参数：

```json
{
  "trigger": {
    "enabled": true,
    "drive": 75,
    "log_path": "D:/Flydigi Space Station/Logs/service_log_{date}.txt",
    "dedup_ms": 300.0,
    "max_ms": 20000.0
  }
}
```

**`log_path` 里的 `{date}` 会自动替换成当天日期**，所以跨天不用改。
如果当天日志还不存在，会**自动回退到最近一份**并在日志里提示。

## 常见问题

**Q：点了启动没反应 / 提示初始化失败**
- 确认手柄已连接、飞智空间站正在运行
- 确认游戏音频设备是显示器/扬声器（程序捕获系统声音）

**Q：滑索时不震动**
- 看日志窗口有没有「滑索开始」字样
- 若完全没有 → 检查 `log_path` 指向的日志文件是否存在
- 若有「滑索开始」但很快「超时结束」→ 调小 `max_ms`

**Q：震动太强/太弱**
- 太强 → 调小「音频强度」或「滑索力度」
- 太弱 → 调大对应滑块

**Q：连续两次滑索被合并成一次**
- 把配置里 `dedup_ms` 从 300 调到 200

**Q：一次滑索被拆成两段**
- 把 `dedup_ms` 从 300 调到 500

## 技术说明

详细的滑索判据原理见同目录 `TRIGGER-GRIP-说明.md`。

简要原理：游戏日志 `ForceTriggerControllerCommandNewXInput` 的一次滑索会产生
**两组信号**（进入 / 脱离），分别对应震动的开始和停止。该信号延迟仅 7ms。
