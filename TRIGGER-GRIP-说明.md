# 扳机联动「滑索」震动 — 判据说明

> 状态：**已实测通过**（2026-09-25 16:21）
> 实现：`src/trigger/source.py`

## 一、结论先行

滑索的**开始**和**结束**，都能从游戏日志中直接读到，**不需要音频、不需要 HID**。

日志 `ForceTriggerControllerCommandNewXInput` 的一次滑索会产生 **两组** 信号：

```
组1（进入滑索）  ──── 滑索持续 3.6 ~ 6.5 秒 ────  组2（脱离滑索）
```

**判定规则：奇数号组 = 开始震动，偶数号组 = 停止震动。**

## 二、实测证据

| 动作 | 开始时刻 | 结束时刻 | 时长 |
|---|---|---|---|
| 滑索 1 | 16:21:47.561 | 16:21:54.023 | **6.46s** |
| 滑索 2 | 16:21:56.403 | 16:22:00.022 | **3.62s** |
| 滑索 3 | 16:22:01.982 | 16:22:05.581 | **3.60s** |
| 滑索 4 | 16:22:07.262 | 16:22:13.701 | **6.45s** |

顺序「长短短长」，**完全跟随操作，无固定时长假设**。

另一组独立测试（62 秒内 4 次）：
`6.43s / 6.49s / 6.43s / 6.44s` —— 波动仅 0.06s。

## 三、信号结构细节

- 每组含 **2 行** 日志，组内间隔 **90 ~ 130ms**（这是"按下"与"程序响应"两条）
- 两组之间的间隔 = 滑索真实时长
- 日志延迟：**中位 7ms / 最大 10ms**（近实时）

组内去重：间隔小于 `dedup_ms`（默认 300ms）的信号视为同一组。

## 四、为什么不用其他方案

| 方案 | 实测结论 |
|---|---|
| 音频 RMS 骤降 | ❌ 滑索音频与 BGM 混叠，分离比仅 1.3~1.4×，切不出边界 |
| 音频低频段（<300Hz） | ❌ 峰值/基线 = 5.6× 看着不错，但滑索段与非滑索段均值只差 1.34× |
| HID `0xFFA0` byte4~11 | ❌ **反映的是「手柄输入活动」而非「滑索状态」**。实测用户只移动视角时也持续非零，会把单次动作拖成 20 秒超时 |
| 固定时长定时器 | ❌ 滑索长短由用户操作决定（3.6s / 6.5s 都会出现） |

### 关于 byte 4~11 的完整结论

它的抖动在"静止 vs 操作手柄"之间有 **40× 区分度**，非常干净：

| 字节 | 静止抖动 | 操作时抖动 | 比值 |
|---|---|---|---|
| 10 | 0.000 | 17.56 | 41.1× |
| 8 | 0.000 | 15.26 | 40.1× |
| 11 | 0.000 | 12.48 | 28.8× |
| 9 | 0.000 | 10.51 | 24.3× |

**但它测的是"你在玩游戏"，不是"滑索在播放"。** 与滑索无因果关系，故不采用。

## 五、配置项

`configs/config.json` → `trigger` 段：

| 字段 | 默认 | 说明 |
|---|---|---|
| `enabled` | `false` | 总开关（命令行 `--trigger` 可覆盖） |
| `drive` | `75` | 握把力度 0-255（冲击峰值约 190~240，故不盖过） |
| `log_path` | 飞智服务日志路径 | 需与空间站实际日志一致 |
| `log_key` | `ForceTriggerControllerCommandNewXInput` | 信号关键词 |
| `poll_ms` | `4.0` | 日志轮询间隔 |
| `dedup_ms` | `300.0` | **组内去重窗口**，间隔小于此值算同一组 |
| `max_ms` | `20000.0` | 兜底超时（防漏掉"脱离"信号） |
| `min_ms` | `150.0` | 最短时长（防瞬时抖动） |
| `use_audio_guard` | `false` | 可选音频安全网，**默认关闭** |

## 六、运行方式

```bash
# 默认（读 configs/config.json）
python -m src.main run --trigger

# 覆盖力度
python -m src.main run --trigger --trigger-drive 90

# 关闭
python -m src.main run
```

## 七、调参建议

- **震动偏弱** → 提高 `drive`（可到 100~120）
- **偶发长时间震动** → 调小 `max_ms`（如 12000）
- **同一次滑索被拆成两段** → 调大 `dedup_ms`（如 500）
- **两次快速滑索被并成一次** → 调小 `dedup_ms`（如 200）

## 八、已知边界

- 若滑索期间游戏不再发送第二组信号（例如中途读盘），会一直震到 `max_ms` 兜底
- 日志文件按日期命名（`service_log_YYYYMMDD.txt`），跨天需更新 `log_path`
